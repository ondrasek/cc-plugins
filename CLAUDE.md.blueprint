# CLAUDE.md

## Quick Reference

```bash
uv sync          # Install dependencies
uv run pytest    # Run tests
```

## Working Conventions

- Tests must pass before completing any task
- `uv run pytest --cov` at the end of every task
