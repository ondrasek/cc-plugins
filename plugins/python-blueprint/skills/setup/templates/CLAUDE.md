# CLAUDE.md
# Generated by python-blueprint setup skill
#
# TEMPLATE: This is a starter CLAUDE.md. The setup skill customizes the
# Quick Reference section based on the detected package manager.

## Quick Reference

```bash
${INSTALL_COMMAND}          # Install dependencies
${PACKAGE_MANAGER_RUN} pytest    # Run tests
```

## Working Conventions

- Tests must pass before completing any task
- `${PACKAGE_MANAGER_RUN} pytest --cov` at the end of every task
