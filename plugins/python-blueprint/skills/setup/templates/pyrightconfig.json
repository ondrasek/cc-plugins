{
  "// TEMPLATE": "Type checker config — generated by python-blueprint setup skill",
  "// NOTE": "This is an EXAMPLE for pyright. Only generate this file if pyright is the chosen type checker. Other type checkers (mypy, ty) configure via pyproject.toml instead.",
  "// VARIABLES": {
    "PYTHON_VERSION": "e.g., 3.13",
    "SOURCE_DIR": "e.g., src"
  },

  "include": ["${SOURCE_DIR}"],
  "exclude": ["**/__pycache__", ".venv"],
  "pythonVersion": "${PYTHON_VERSION}",
  "typeCheckingMode": "standard",
  "reportMissingImports": true,
  "reportMissingTypeStubs": false
}
