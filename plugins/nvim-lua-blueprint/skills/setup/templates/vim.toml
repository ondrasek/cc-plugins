# Selene standard library definition for Neovim globals
# Generated by nvim-lua-blueprint setup skill
#
# THIS IS AN ANNOTATED EXAMPLE. This file tells selene about Neovim's
# global namespace so it doesn't flag vim.* as undefined. The setup skill
# should verify this covers the APIs the project uses.
#
# The "vim" std extends Lua 5.1 (LuaJIT) with Neovim-specific globals.
# See: https://github.com/Kampfkarren/selene/blob/main/docs/src/usage/std.md

[selene]
base = "lua51"
name = "vim"

[vim]
any = true
