# CLAUDE.md
# Generated by go-blueprint setup skill
#
# TEMPLATE: This is a starter CLAUDE.md. The setup skill customizes the
# Quick Reference section based on the detected project structure.

## Quick Reference

```bash
go build ./...          # Build project
go test ./...           # Run tests
golangci-lint run ./... # Run linter
golangci-lint fmt ./... # Format code
```

## Working Conventions

- Tests must pass before completing any task
- `go test ./...` at the end of every task
- All new exported symbols must have doc comments
- Run `go mod tidy` after adding/removing dependencies
