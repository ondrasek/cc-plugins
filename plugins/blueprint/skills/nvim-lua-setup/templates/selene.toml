# Selene linter configuration for Neovim Lua plugins
# Generated by nvim-lua-blueprint setup skill
#
# THIS IS AN ANNOTATED EXAMPLE. The setup skill should adjust based on
# the project's needs. What matters:
#   - std = "vim" tells selene to use the accompanying vim.toml for Neovim globals
#   - Without this, selene will flag all vim.* usage as undefined globals
#
# TEMPLATE VARIABLES: None — this file is relatively static.
# The companion vim.toml defines the Neovim global namespace.

std = "vim"
