# StyLua formatter configuration for Neovim Lua plugins
# Generated by nvim-lua-blueprint setup skill
#
# THIS IS AN ANNOTATED EXAMPLE. The setup skill should adjust column_width,
# indent_width, and quote_style based on the project's existing style or
# user preference. What matters:
#   - lua_version must be "luajit" for Neovim plugins
#   - call_parentheses = "Always" is the Neovim community convention
#
# TEMPLATE VARIABLES (replaced by setup skill):
#   ${COLUMN_WIDTH}   — max line width (e.g., 120)
#   ${INDENT_WIDTH}   — spaces per indent level (e.g., 2)
#   ${QUOTE_STYLE}    — quote style (e.g., "AutoPreferDouble")

column_width = ${COLUMN_WIDTH}
line_endings = "Unix"
indent_type = "Spaces"
indent_width = ${INDENT_WIDTH}
quote_style = "${QUOTE_STYLE}"
call_parentheses = "Always"
lua_version = "luajit"
