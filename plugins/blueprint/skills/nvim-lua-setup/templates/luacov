-- luacov configuration for Neovim Lua plugins
-- Generated by nvim-lua-blueprint setup skill
--
-- THIS IS AN ANNOTATED EXAMPLE. The setup skill should adjust include/exclude
-- patterns based on the project's structure. What matters:
--   - Include the plugin's source modules
--   - Exclude test files and external dependencies
--
-- TEMPLATE VARIABLES (replaced by setup skill):
--   ${PLUGIN_NAME}  — plugin module name for include filter

return {
  -- Only measure coverage for the plugin's own code
  include = {
    "${PLUGIN_NAME}",
    "${PLUGIN_NAME}/.*",
  },

  -- Exclude test files and dependencies
  exclude = {
    "tests/.*",
    "spec/.*",
    "luacov/.*",
  },

  -- Report file location
  reportfile = "luacov.report.out",
  statsfile = "luacov.stats.out",
}
